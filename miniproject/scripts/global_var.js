//actually, only one global variable use (умовно, знаю, що функція), USERS_URL. Another only initialized
let USERS_URL =() => {return  'https://jsonplaceholder.typicode.com/users'};

let usersList = [];
let postList=[];
let commentsList=[];